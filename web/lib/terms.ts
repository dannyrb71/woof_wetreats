// Single source of truth for the current Terms of Service version.
// Bump this whenever the /terms content changes — it's stored on each
// reservation so we have a record of exactly what was agreed to.
// Kept simple (the Terms' effective date) per requirements — not a full
// versioning system.
export const TERMS_VERSION = '2026-06-21'
